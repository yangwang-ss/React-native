import React from 'react';
import { StyleSheet, View } from 'react-native';

export default class ShareCanvas extends React.Component {
  render() {
    return (
      <View />
    );
  }
}

const styles = StyleSheet.create({
});
